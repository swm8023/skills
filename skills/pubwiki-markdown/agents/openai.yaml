interface:
  display_name: "PubWiki Markdown"
  short_description: "Create, organize, and publish Wiki Markdown"
  default_prompt: "Use $pubwiki-markdown to organize this durable knowledge into the local WheelMaker Wiki."

policy:
  allow_implicit_invocation: true
